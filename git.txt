conectar ao git

git init
git branch -M main
git remote add origin url

git add .
git status
git commit -m "msg"
git log
git push origin main

git pull origin main
git log

git branch
git checkout -b testes
git status
git push origin testes

git branch
git checkout main
git merge testes
git brach -D testes
git push origin testes --delete

cria commit apos resolver o conflito

ISSUES
SOLICITAÇÃO DE TAREFAS


WORK FLOW PARA TESTES CI

